# Examples

Place short penalty-kick clips here for local testing, for example:

```text
examples/penalty_clip.mp4
```

Short clips are recommended for CPU inference. The repository does not include sample match footage because video rights and licensing need to be handled separately.

